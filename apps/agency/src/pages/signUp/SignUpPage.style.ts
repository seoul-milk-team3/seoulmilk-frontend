import { css } from '@emotion/react';
import { theme } from '@seoulmilk/styles';

export const detailStyle = css({
  color: theme.colors.grayscale_70,
});

export const detailLoginStyle = css({
  color: theme.colors.grayscale_70,
  textDecorationLine: 'underline',
});
